<h3>Detalhe do Pet</h3>

<br>

<%= render partial: 'pet', locals: {pet: @pet} %>

<br>
<%= link_to 'Ver Todos', pets_path, class: 'btn btn-default' %>
<%= link_to 'Editar', edit_pet_path(@pet), class: 'btn btn-info' %>
<%= link_to 'Excluir', pet_path(@pet), method: :delete, data: { confirm: 'Tem certeza?' }, class: 'btn btn-danger' %>